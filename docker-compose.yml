# version: "3"
# services:
#   aut-server:
#     build: .
#     ports:
#       - "5000:5000"
#     depends_on:
#       - react-app
#   react-app:
#     build: ../client
#     ports:
#       - "3000:3000"
version: "3"
services:
  server:
    build: ./server
    ports:
      - "5000:5000"

  client:
    build: ./client
    ports:
      - "3000:3000"
